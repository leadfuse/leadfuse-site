// Placeholder for the Claude Design project's shared site.js (motion helpers).
// The pages call initMotion(); a no-op keeps them error-free until the real file is dropped in.
export function initMotion() {}
export default { initMotion };
